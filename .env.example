# Copy to `.env` for local development.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Only needed when the database requires SSL (Render *External* URL, Neon, Supabase, …).
# Render's internal URL (used by render.yaml) does NOT need this.
# DATABASE_SSL=true
